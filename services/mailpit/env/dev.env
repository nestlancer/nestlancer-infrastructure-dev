# Mailpit Dev Configuration
MP_MAX_MESSAGES=5000
MP_DATABASE=/data/mailpit.db

# UI Credentials
MAILPIT_UI_USER=admin
MAILPIT_UI_PASS=nestlancer-mail-secure

MP_SMTP_BIND_ADDR=0.0.0.0:1025
MP_HTTP_BIND_ADDR=0.0.0.0:8025
